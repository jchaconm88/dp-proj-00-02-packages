name: Publish packages (@jchaconm88/*) to GitHub Packages

on:
  push:
    tags:
      - "packages-v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          registry-url: "https://npm.pkg.github.com"
          scope: "@jchaconm88"
          cache: "npm"

      - name: Install
        run: npm ci

      - name: Build workspaces
        run: npm run build

      - name: Publish @jchaconm88/shared
        run: npm publish -w @jchaconm88/shared
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish @jchaconm88/ui
        run: npm publish -w @jchaconm88/ui
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
